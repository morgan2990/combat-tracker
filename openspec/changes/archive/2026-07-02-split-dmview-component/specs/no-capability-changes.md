## No Capability Changes

This change is a pure internal restructuring (see `proposal.md`'s Capabilities section): no new capabilities, no requirement-level behavior changes to any existing capability. There are no delta spec files because there is nothing to add, modify, remove, or rename in `openspec/specs/`.
